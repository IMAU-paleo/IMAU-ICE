&CONFIG

fixed_output_dir_suffix_config              = '_040_hybrid_sans'                    ! Suffix to put after the fixed output directory name, useful when doing ensemble runs with the template+variation set-up

ISMIP_HOM_L_config                          =   40000.0                        ! Domain size of the ISMIP-HOM benchmarks
xmin_ANT_config                             =  -20000.0                        ! Western  boundary     of the Antarctica domain [m]
xmax_ANT_config                             =   20000.0                        ! Eastern  boundary     of the Antarctica domain [m]
ymin_ANT_config                             =  -20000.0                        ! Southern boundary     of the Antarctica domain [m]
ymax_ANT_config                             =   20000.0                        ! Northern boundary     of the Antarctica domain [m]
dx_ANT_config                               =     500.0                        ! Horizontal resolution of the Antarctica domain [m]

choice_ice_dynamics_config                  = 'SIA/SSA'                        ! Choice of ice-dynamica approximation: "none" (= fixed geometry), "SIA", "SSA", "SIA/SSA", "DIVA"
include_SSADIVA_crossterms_config           = .FALSE.                          ! Whether or not to include the "cross-terms" of the SSA/DIVA

/
